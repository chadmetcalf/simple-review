%h1 Listing reviews

%table
  %thead
    %tr
      %th Creator
      %th Reviewer
      %th Reviewee
      %th Rubric
      %th Active
      %th Document
      %th
      %th
      %th

  %tbody
    - @reviews.each do |review|
      %tr
        %td= review.creator_id
        %td= review.reviewer_id
        %td= review.reviewee_id
        %td= review.rubric_id
        %td= review.active
        %td= review.document
        %td= link_to 'Show', review
        %td= link_to 'Edit', edit_review_path(review)
        %td= link_to 'Destroy', review, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Review', new_review_path
